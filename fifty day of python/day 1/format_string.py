# As we learned in the Python Variables chapter, we cannot combine strings and numbers directlt
# we use format string(f-strings) method
# To specify a string as an f-string, simply put an f in front of the string literal, and add curly brackets {} as placeholders for variables and other operations.


b=20

c=f"my name is nabin basnet. and i am {b} years old"

print(c)