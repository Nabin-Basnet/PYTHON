# A lambda function in Python is an anonymous, small function defined using 
# the lambda keyword, which can have any number of arguments but only one expression, 
# and returns the result of that expression.

sum=lambda x,y:x+y
square=lambda x:x*x

print(sum(5,10))
print(square(10))